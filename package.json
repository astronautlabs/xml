{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "clean": "rimraf --glob packages/*/dist packages/*/dist.esm"
  },
  "devDependencies": {
    "lerna": "^8.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
